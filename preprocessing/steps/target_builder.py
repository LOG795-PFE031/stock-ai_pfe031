from ..abstract import BaseDataProcessor


class TargetBuilder(BaseDataProcessor):

    def __init__(self, symbol, logger):
        super().__init__(symbol, logger)

    # TODO Implementation
    def process(self, data):
        return super().process(data)
